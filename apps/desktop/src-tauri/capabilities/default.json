{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Lets the Falcon panel listen to app events (mic level/status, transcript).",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
